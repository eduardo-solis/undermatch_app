import 'package:flutter/material.dart';

class EquiposConsultar extends StatelessWidget {
  const EquiposConsultar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Consultar")),
    );
  }
}
